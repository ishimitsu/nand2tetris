import os
import sys

class Code:
    
    def dest(mnemonic):
        ret = 0 # 3bit
        return ret

    def comp(mnemonic):
        ret = 0 # 7bit
        return ret

    def jump(mnemonic):
        ret = 0 # 3bit
        return ret
    
        
             
